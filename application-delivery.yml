spring:
  application:
    name: delivery-service

  datasource:
    url: jdbc:mysql://localhost:3306/deliverydb
    username: root
    password: drisht123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8086